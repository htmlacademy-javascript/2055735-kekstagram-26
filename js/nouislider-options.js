const ChromeSlider = {
  connect: 'lower',
  range: {
    min: 0,
    max: 1,
  },
  start: 1,
  step: 0.1,
};

const SepiaSlider = {
  connect: 'lower',
  range: {
    min: 0,
    max: 1,
  },
  start: 1,
  step: 0.1,
};

const MarvinSlider = {
  connect: 'lower',
  range: {
    min: 0,
    max: 100,
  },
  start: 100,
  step: 1,
};

const PhobosSlider = {
  connect: 'lower',
  range: {
    min: 0,
    max: 3,
  },
  start: 3,
  step: 0.1,
};

const HeatSlider = {
  connect: 'lower',
  range: {
    min: 1,
    max: 3,
  },
  start: 3,
  step: 0.1,
};

export {
  ChromeSlider,
  SepiaSlider,
  MarvinSlider,
  PhobosSlider,
  HeatSlider
};
